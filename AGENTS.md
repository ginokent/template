# AGENTS.md

## ルール

- 一切妥協しないこと
- 一切忖度しないこと
- 批判的思考で対話し、誤りには反論すること
- 常に日本語を利用すること
- 全角と半角の間には半角スペースを入れること
- コメントは全て日本語にすること
- ログメッセージは全て英語にすること
- エラーメッセージは全て英語にすること

- 可能な限り Subagent に委譲してコンテキストを節約
- 不明点は AskUserQuestion Tool で積極的に確認

## 設計について

- **後方互換性・fallback は考慮不要**。セマンティクスのクリーンさを優先
- 既存テストの粒度に引きずられず **常に適切なテストを書く**
- 「堅牢性/UX 優先」か「現実的」かの選択は必ずユーザーに確認
- オーバーエンジニアリングを避ける
  - オーバーエンジニアリングかどうかの判断はユーザーに確認

## ドキュメンテーションについて

- 指定なき場合は日本語で記述
- 大方針設計は `SPEC.md` に記述
- 詳細設計はコード内コメントに記述
- `AGENTS.md` `README.md` `SPEC.md` が存在する場合はそれぞれ読んで概要を把握
- 変更をした場合、変更点とドキュメントの整合性を確認すること

## テストについて

### テストの役割分担

- PBT: 型情報 (Strategy) に基づいて入力を生成し、プロパティを検証する (ラウンドトリップ等)
- Fuzzing: 任意入力に対するクラッシュ耐性 (パニック安全性)
- 単体テスト: 意図的なエラーパス、境界値など PBT で実現できないケース
- PBT でカバーできるものを単体テストで書かない

## git ブランチの命名規則

- Git Flow を使うこと
- ブランチ名は `feature/add-xxx` または `feature/update-xxx` または `feature/fix-xxx` または `feature/breaking-change-xxx` にすること
  - 機能追加は prefix を `feature/add-` でブランチを切って対応すること
  - バグ修正は prefix を `feature/fix-` でブランチを切って対応すること
  - 後方互換のある変更は prefix を `feature/update-` でブランチを切って対応すること
  - 後方互換のない変更は prefix を `feature/breaking-change-` でブランチを切って対応すること
- ブランチ名に issue の番号を含めないこと

## コミットについて

- 意味のある単位でコミットすること
- 説明文の末尾に句点 (。) は付けない
- 本文は任意 (変更内容が複雑な場合や補足説明が必要な場合のみ使用)
- 本文は箇条書き (`-` から開始) で記述する
- 何がどのように変更されたかを具体的に記述する
- 変更の動機や背景 (何がきっかけか、どの問題を解決するのか) を含めること — ただし、自明な場合は省略可能
- 言い回しに厳密な決まりはない。「〜のために」のような固定パターンでなくても、動機が明確であれば問題ない
- 抽象的な表現は避け、可能な限り具体的な識別子 (関数名、メソッド名、構造体名、インターフェース名、ファイル名など) を使用する
- スコープの指定は任意だが、変更箇所が限定的な場合は推奨される
- 言語別のルール:
  - `en` (英語) :
    - 説明文: 命令形を使用する (「Added」ではなく「Add」)
    - 本文: 命令形の箇条書きを使用する (「- Add X」「- Fix Y」)
    - 例: "Fix expiry check in SessionManager.validate() to resolve login failure"
    - 例: "Add RedisCache adapter for faster API response"
    - 例: "Add FindByEmail method to UserRepository interface"
    - 例: "Update README"  (動機が自明のため省略)
  - `ja` (日本語) :
    - 説明文: 動詞終止形を使用する (「〜を追加した」や「〜を追加」ではなく「〜を追加する」)
    - 本文: 非敬語・体言止め・名詞句を使用する (「〜を追加」「〜の修正」「〜を更新」) 。「〜する」「〜した」「〜します」は使用不可
    - スペース: 全角文字と半角文字の間には半角スペースを入れる (例: 「SKILL.md に body ルールを追加する」、誤例: 「SKILL.md にbodyルールを追加する」)
    - 例: 「ログイン失敗を解消するために SessionManager.validate() の期限切れ判定を修正する」
    - 例: 「レスポンス高速化のため RedisCache アダプタを追加する」
    - 例: 「UserRepository インターフェースに FindByEmail メソッドを追加する」
    - 例: 「README を更新する」 (動機自明のため省略)

### コミットメッセージの形式

```
<type>(<任意のアドバイス>): <説明>

[任意の本文]
- <詳細 1>
- <詳細 2>
```

### タイプ

- feat: 新機能
- fix: バグ修正
- docs: ドキュメントのみの変更
- style: フォーマットの変更（コードの動作に影響しない修正）
- refactor: リファクタリング（機能追加やバグ修正を含まないコードの整理）
- test: テストの追加や修正
- chore: その他、ビルドプロセスや補助的なツールなどの変更
- perf: パフォーマンスの改善
- ci: CI/CD設定の変更
- build: ビルドシステムに関わる変更

## issue について

- `issues/` 配下で管理すること
- 形式は https://raw.githubusercontent.com/ginokent/template/refs/heads/develop/issues/0000-00-00-00-00-template.md を参考にすること
- issue の id は JST の起票時刻 `YYYY-mm-dd-HH-MM` とすること
- {id}-{conventional-commit-type}-{short-description}.md という命名規則を守ること
  - 例: 2026-05-26-18-17-feat-add-user-model.md
  - 例: 2026-05-25-17-16-fix-fix-parser-error.md
- issue を作成したら作業に取り掛かる前にまずコミットすること
- issue をコミットするときはコミットメッセージに issue の id とタイトルを記載すること
- 完了した issue は `issues/completed/` 配下に移動すること
- 完了以外の理由で終了した issue は `issues/not-planned/` 配下に移動すること
- issue を移動したらコミットすること

## PR について

- PR 作成時や PR 本文更新時は、本文にセッション URL を **付与しない** こと
